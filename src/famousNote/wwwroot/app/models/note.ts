export class Note {
    constructor(
        id: String,
        title: String,
        note: String,
        active: Boolean,
        published: Boolean,
        userId: String,
        createdOn: Date,
        updatedOn: Date,
        publishedOn: Date
    ) { }
}